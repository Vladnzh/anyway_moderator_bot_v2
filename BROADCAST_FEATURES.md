# Функции массовой рассылки

## Обзор

Модуль массовой рассылки позволяет отправлять сообщения всем пользователям, зарегистрированным в Supabase и имеющим привязанный Telegram аккаунт.

## Новые возможности (2025-12-12)

### ✅ Исправленные ошибки

1. **Исправлена ошибка с функцией `checkSupabaseConnection`**
   - Функция теперь корректно объявлена и доступна в HTML
   - Автоматическая проверка подключения при открытии вкладки "Рассылка"

2. **Исправлена ошибка с функцией `loadBroadcastPreview`**
   - Функция корректно загружает список получателей
   - Улучшена обработка ошибок

### 🆕 Новые функции

#### 1. Просмотр подключенных пользователей

Кнопка **"🔍 Загрузить предпросмотр"** позволяет:
- Увидеть полный список пользователей с привязанным Telegram
- Просмотреть информацию: Telegram ID, Username, Email, Имя
- Узнать точное количество получателей перед рассылкой
- Быстро отправить тестовое сообщение конкретному пользователю

#### 2. Отправка тестового сообщения

Кнопка **"📨 Отправить тест"** позволяет:
- Отправить тестовое сообщение одному пользователю перед полной рассылкой
- Проверить форматирование и содержание
- Убедиться, что бот корректно работает

**Два способа отправки теста:**

1. **Через главную кнопку "📨 Отправить тест":**
   - Нажмите кнопку
   - Введите Telegram User ID получателя
   - Введите или отредактируйте текст сообщения
   - Нажмите "Отправить"

2. **Через список пользователей:**
   - Загрузите предпросмотр получателей
   - Нажмите кнопку "📨 Тест" напротив нужного пользователя
   - ID и username уже будут заполнены автоматически
   - Отредактируйте сообщение при необходимости
   - Нажмите "Отправить"

## API Endpoints

### POST `/api/broadcast/test`

Отправить тестовое сообщение одному пользователю.

**Запрос:**
```json
{
  "tg_user_id": 123456789,
  "message": "Тестовое сообщение",
  "parse_mode": "HTML"  // опционально: "HTML", "Markdown" или null
}
```

**Ответ (успех):**
```json
{
  "success": true,
  "message": "Тестовое сообщение успешно отправлено",
  "data": {
    "message_id": 12345,
    "chat": { ... },
    ...
  }
}
```

**Ответ (ошибка):**
```json
{
  "success": false,
  "message": "Ошибка отправки: chat not found"
}
```

### POST `/api/broadcast/preview`

Получить список пользователей для рассылки (без отправки).

**Запрос:**
```json
{
  "filters": null  // в будущем здесь будут фильтры
}
```

**Ответ:**
```json
{
  "success": true,
  "users": [
    {
      "tg_user_id": "123456789",
      "username": "john_doe",
      "email": "john@example.com",
      "full_name": "John Doe"
    },
    ...
  ],
  "count": 42,
  "message": "Найдено 42 пользователей с привязанным Telegram"
}
```

### POST `/api/broadcast/send`

Отправить массовую рассылку всем пользователям.

**Запрос:**
```json
{
  "message": "Привет всем! Это массовая рассылка.",
  "parse_mode": "HTML",  // опционально
  "filters": null  // в будущем здесь будут фильтры
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Рассылка завершена: отправлено 40, ошибок 2",
  "data": {
    "total": 42,
    "success": 40,
    "failed": 2,
    "failed_users": [
      {
        "tg_user_id": "123",
        "username": "blocked_user",
        "error": "Forbidden: bot was blocked by the user"
      }
    ]
  }
}
```

## Использование через интерфейс

1. **Перейдите на вкладку "📢 Рассылка"**
   - Автоматически проверится подключение к Supabase

2. **Проверьте подключение**
   - Должен быть статус "Подключено к Supabase ✓"
   - Если нет - проверьте .env файл (DB_HOST, DB_PASSWORD и т.д.)

3. **Загрузите предпросмотр получателей**
   - Нажмите "🔍 Загрузить предпросмотр"
   - Просмотрите список пользователей

4. **Отправьте тестовое сообщение** (опционально)
   - Выберите пользователя из списка и нажмите "📨 Тест"
   - Или используйте главную кнопку "📨 Отправить тест"
   - Проверьте, что сообщение получено и отображается корректно

5. **Создайте сообщение для рассылки**
   - Введите текст в поле "Текст сообщения"
   - Выберите форматирование (HTML, Markdown или без форматирования)

6. **Отправьте рассылку**
   - Нажмите "📤 Отправить рассылку"
   - Подтвердите действие
   - Дождитесь завершения и проверьте результаты

## Требования

- **Supabase подключение:** DB_HOST, DB_PASSWORD, DB_USER, DB_NAME в .env
- **Telegram Bot Token:** BOT_TOKEN в .env
- **Таблица users в Supabase** с полями:
  - `tg_user_id` (bigint) - обязательно
  - `username` (text) - опционально
  - `email` (text) - опционально
  - `full_name` (text) - опционально

## Ограничения Telegram API

- **Rate limiting:** Бот автоматически делает задержки между сообщениями (50ms)
- **Максимальная длина сообщения:** 4096 символов
- **Блокировки:** Некоторые пользователи могли заблокировать бота - они будут отображены в ошибках

## Безопасность

- Все эндпоинты требуют авторизации через Bearer токен
- Подтверждение перед отправкой массовой рассылки
- Логирование всех операций
- Защита от SQL-инъекций (параметризованные запросы)

## Планы развития

См. TODO_BROADCAST_DATA.md для списка запланированных улучшений:
- Фильтры пользователей
- Планирование рассылок
- Шаблоны сообщений
- Статистика и аналитика
