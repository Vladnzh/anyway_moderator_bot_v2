# 🧪 Тестирование бота

Комплексные тесты для проверки работоспособности модератор-бота после оптимизаций.

## 📋 Доступные тесты

### 1. 🔄 Workflow тест (`test_workflow.py`)
**Что тестирует:**
- Создание тестовых тегов (автоматические и с модерацией)
- Симуляцию 10 автоматических сообщений
- Симуляцию 20 сообщений для модерации
- Обработку модерации: 10 одобрений + 10 отклонений
- Проверку логов и статистики в базе данных

**Проверяет:**
- ✅ Полный цикл обработки сообщений
- ✅ Работу модерации через API
- ✅ Корректность записи в базу данных
- ✅ Обработку очереди реакций

### 2. ⚡ Performance тест (`test_performance.py`)
**Что тестирует:**
- Создание 30 элементов модерации
- Быстрые последовательные одобрения (10 шт.)
- Параллельные одобрения с concurrency=5 (20 шт.)
- Обработку очереди реакций под нагрузкой

**Проверяет:**
- ✅ Исправление бага с пропуском реакций при быстром апруве
- ✅ Стабильность при параллельных запросах
- ✅ Производительность API
- ✅ Работу семафора для ограничения параллельности

## 🚀 Как запустить тесты

### Предварительные требования
1. **Админка должна быть запущена:**
   ```bash
   ./start.sh  # или python admin.py
   ```

2. **Установлены зависимости:**
   ```bash
   pip install aiohttp
   ```

3. **Настроены переменные окружения:**
   ```bash
   export ADMIN_TOKEN="your_admin_token"
   # или в .env файле
   ```

### Запуск всех тестов
```bash
python run_tests.py
```

### Запуск отдельных тестов
```bash
# Workflow тест
python test_workflow.py

# Performance тест  
python test_performance.py
```

## 📊 Интерпретация результатов

### ✅ Успешный результат
- **Workflow тест:** 90%+ успешности, минимум ошибок
- **Performance тест:** 20+ успешных запросов, стабильная обработка очереди
- **Общий вывод:** "ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!"

### ⚠️ Результат с замечаниями  
- **Workflow тест:** 70-90% успешности, есть ошибки но основное работает
- **Performance тест:** 15-20 успешных запросов, частичные проблемы
- **Общий вывод:** "БОЛЬШИНСТВО ТЕСТОВ ПРОЙДЕНО"

### ❌ Критические проблемы
- **Workflow тест:** <70% успешности, много ошибок
- **Performance тест:** <15 успешных запросов, системные ошибки  
- **Общий вывод:** "КРИТИЧЕСКИЕ ПРОБЛЕМЫ"

## 🔍 Что проверяют тесты

### Исправление критических багов:
- ✅ **Баг с пропуском реакций** при быстром апруве (семафор)
- ✅ **Race conditions** при параллельных запросах
- ✅ **Блокировки базы данных** (WAL mode)

### Производительность:
- ✅ **Кэширование тегов** (ускорение запросов)
- ✅ **Оптимизации SQLite** (параллельность)
- ✅ **Обработка очереди реакций** (стабильность)

### Функциональность:
- ✅ **API админки** (все endpoints)
- ✅ **Модерация** (одобрение/отклонение)
- ✅ **Логирование** (корректность данных)
- ✅ **Статистика** (подсчет метрик)

## 🐛 Устранение проблем

### Админка недоступна
```
❌ Админка недоступна по адресу: http://localhost:8000
```
**Решение:** Запустите админку `python admin.py` или `./start.sh`

### Ошибки авторизации
```
❌ HTTP 401 для item_id: Unauthorized
```
**Решение:** Проверьте `ADMIN_TOKEN` в переменных окружения

### Проблемы с базой данных
```
❌ Ошибка подключения к базе данных
```
**Решение:** Проверьте права доступа к файлу `bot_data.db`

### Медленная работа
```
⚠️ Среднее время ответа: 2.5с
```
**Решение:** Проверьте нагрузку на систему, перезапустите админку

## 📈 Ожидаемая производительность

После оптимизаций:
- **Время ответа API:** 0.1-0.5 секунд
- **Параллельные запросы:** до 5 одновременно
- **Обработка модерации:** 20-30 элементов/минуту
- **Пропускная способность:** 30-40 сообщений/секунду

## 🔧 Настройка тестов

### Изменение параметров в `test_workflow.py`:
```python
# Количество сообщений
await self.simulate_automatic_messages(10)      # автоматические
await self.simulate_moderation_messages(20)     # для модерации

# Соотношение одобрений/отклонений  
await self.process_moderation_queue(approve_count=10, reject_count=10)
```

### Изменение параметров в `test_performance.py`:
```python
# Количество элементов для стресс-теста
item_ids = await self.create_test_moderation_items(30)

# Уровень параллельности
concurrent_time = await self.stress_test_concurrent_approvals(remaining_items, concurrency=5)
```

## 📞 Поддержка

При проблемах с тестами:
1. Проверьте логи админки
2. Убедитесь что бот не запущен одновременно с тестами
3. Очистите базу данных при необходимости: `rm bot_data.db`
4. Перезапустите админку после изменений

---

**Важно:** Тесты создают тестовые данные в базе. После тестирования рекомендуется очистить базу или использовать отдельную тестовую базу.
